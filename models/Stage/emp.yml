version: 2

models:
  - name : employeesmpg
    description : "employees"
    columns:
      - name : employee_no
        description : "employeesnumber"
        tests:
         - unique
         - not_null
  - name : departments
    description : "dep"
    columns:
      - name : department_id
        description : "employeesdep"
        tests:
         - unique
         - not_null